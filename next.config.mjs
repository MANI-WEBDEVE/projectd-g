/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["digitalpencil.tech", "images.unsplash.com", 'assets.aceternity.com', "images.pexels.com"],
  },
};

export default nextConfig;
